PORT=8080
CORS_ORIGIN=*
# Set this to the n8n webhook production URL:
N8N_WEBHOOK_URL=
# Optional shared secret. Must match n8n's X-Webhook-Secret check if you add one.
N8N_WEBHOOK_SECRET=
# Optional API key. If set, frontend/backend requests must send X-API-Key.
API_KEY=
